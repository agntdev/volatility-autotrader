BOT_TOKEN=
# Optional: Deriv application id (default 1089 — public demo app)
DERIV_APP_ID=1089
# Optional: secret used to encrypt per-user Deriv API tokens at rest
TOKEN_ENCRYPTION_KEY=
# Optional: Telegram user id allowed to change owner settings
BOT_OWNER_ID=
# Optional: Redis URL for durable data + sessions in production
REDIS_URL=
